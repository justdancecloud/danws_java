signing.gnupg.executable=gpg
# signing.gnupg.keyName can be set here, or via the GPG_KEY_NAME environment
# variable (preferred for CI/shared machines so the key identifier isn't
# committed to the repo). The default below matches the justdancecloud key.
signing.gnupg.keyName=433C6793
